# ios platform folder

From the parent `mobile/` directory:

    flutter create --org com.azieeliab --project-name azbot .

Then `flutter pub get` and `flutter run`. Bundle id: `com.azieeliab.azbot`.
Offline viewer. Not a store IPA.
